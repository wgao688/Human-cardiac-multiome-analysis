### Subdirectories

There are two subdirectories. `binarized` contains DEG analysis for diseased donors vs. non-diseased donors, including all diseased donors in a binarized manner. `non_binarized` individually compares DCM, HCM, and ICM donors against all non-diseased donors.
